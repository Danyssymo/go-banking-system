# Собранные бинарники
/bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Локальные переменные окружения (секреты)
.env
.env.local

# go.work.sum — генерируется локально, зависит от машины разработчика
go.work.sum

# IDE
.idea/

# Локальный журнал прогресса — личные заметки, не для репозитория
/PROGRESS.md
